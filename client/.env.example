# Base URL of the QuoteCompass API.
# Leave empty for local dev (Vite proxies /api to http://localhost:4000).
# In production (Vercel), set this to your deployed API URL, e.g. https://quote-compass-api.onrender.com
VITE_API_BASE=
